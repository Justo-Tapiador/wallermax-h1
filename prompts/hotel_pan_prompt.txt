D:\github\repositories\wallermax-h1>findstr /N /C:"FormData" /C:"formData" /C:"referenceImage" /C:"finalImage" /C:"userAtlas" /C:"/api/pipeline" "D:\github\repositories\wallermax-h1\public\app.js"
4: *   - Read form state, build a multipart/form-data POST to /api/pipeline.
18:    refInput: $("referenceImage"),
19:    finalInput: $("finalImage"),
211:    const fd = new FormData();
223:    if (el.refInput.files && el.refInput.files[0]) fd.append("referenceImage", el.refInput.files[0]);
224:    if (el.finalInput.files && el.finalInput.files[0]) fd.append("finalImage", el.finalInput.files[0]);
227:      const r = await fetch("/api/pipeline", { method: "POST", body: fd });
249:    const fd = new FormData();
332:    if (p.finalImageAnalysis) el.finalJson.textContent = JSON.stringify(p.finalImageAnalysis, null, 2);
476:    if (j.finalImageAnalysis) el.finalJson.textContent = JSON.stringify(j.finalImageAnalysis, null, 2);
557:  userAtlas: {},  // { "row,col": { source_atlas, source_row, source_col } }
697:  atlasState.userAtlas[nextSlot] = {
719:      if (!atlasState.userAtlas[`${row},${col}`]) return `${row},${col}`;
742:  if (!atlasState.userAtlas[key]) return;
743:  delete atlasState.userAtlas[key];
755:  const count = Object.keys(atlasState.userAtlas).length;
764:  Object.entries(atlasState.userAtlas).forEach(([coord, info]) => {
784:  Object.keys(atlasState.userAtlas).forEach(key => {
792:  const count = Object.keys(atlasState.userAtlas).length;
798:  const tiles = Object.entries(atlasState.userAtlas).map(([coord, info]) => {
808:  const userAtlasJson = JSON.stringify({
816:  document.getElementById("userAtlasJson").value = userAtlasJson;
819:  console.log(`[atlas] userAtlas serialized with ${count} tiles`);